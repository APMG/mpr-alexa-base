{
  "name": "mpr-alexa-base",
  "version": "0.0.1",
  "description": "A Base Configurable Set of Alexa Skill Handlers for Streaming Audio",
  "author": "Minnesota Public Radio",
  "license": "MIT",
  "dependencies": {
    "alexa-sdk": "^1.0.14",
    "axios": "^0.16.2",
    "lodash.get": "^4.4.2"
  },
  "scripts": {
    "build": "webpack",
    "zip": "cd dist && zip ../alexa-bundle.zip * && cd ../",
    "deploy": "yarn run build && yarn run zip && yarn run upload",
    "upload": "echo 'Uploading needs to be implemented!!!!!!!!!!!'"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-loader": "^7.1.2",
    "babel-preset-env": "^1.6.0",
    "babel-preset-es2016": "^6.24.1",
    "webpack": "^3.7.1"
  }
}
